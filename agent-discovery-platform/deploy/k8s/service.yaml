apiVersion: v1
kind: Service
metadata:
  name: agent-discovery-backend
spec:
  selector:
    app: agent-discovery-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
